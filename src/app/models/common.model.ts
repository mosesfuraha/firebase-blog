export interface IExpense {
  key?: string;
  title: string;
  description: string;
  price: string;
}
export interface Blog {
  imageUrl: string;
  title: string;
  description: string;
  category: string;
  date: string;
  authorName: string;
  authorId: string;
}
